{
// Configure your Run and Preview buttons here.

// Run button configuration
  "commands": {
    "Start server": "npm install && node main.js",
    "Deploy": "git checkout gh-pages && git merge --no-ff master && git push && git checkout master",
    "Node version": "node --version"
  },

// Preview button configuration
  "preview": {
    "Box URL": "http://{{domain}}:3000/?subject=testing-with-meteor"
  }
}